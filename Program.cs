using System;

namespace Objectorientation
{
    class Program
    {
        //   Hver klasse består av:
        // - Properties som brukes til å beskrive egenskaper ved objektet
        // - Properties som sier noe om hva et objekt inneholder 
        // - Metoder som sier noe om hva objektet kan gjøre

        // Pause til 11

        /* 
         * Hvert klasse har en lærer.

         * Lag kode som kan produsere timeplan for en elev og for en lærer. 
         * Lag lister over elev for hvert fag - og for hver klasse. 
         * Noen fag er felles for en klasse - andre fag har elever fra forskjellige klasser. */

        static void Main(string[] args)
        {
            //    var teacher = new Lærer("Marie");
            //    teacher.ChangeName("Tonje");

            var school = new Skole();

        }
    }
}
